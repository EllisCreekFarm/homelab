---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/app-template-3.6.0/charts/other/app-template/schemas/helmrelease-helm-v2beta2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grafana-oncall-initialize
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.6.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      migrate:
        type: job
        replicas: 1
        annotations:
          reloader.stakater.com/auto: "true"
        job:
          backoffLimit: 15
          completions: 1
          ttlSecondsAfterFinished: 20
        containers:
          app:
            image:
              repository: alpine/curl
              tag: 8.11.1@sha256:21307f80a0ae6d4e49b67028539182ac0aba2948a91aa2d2d98cdfd2f5f9694d
            command: [ "/scripts/initialize.sh" ]
            env:
              GRAFANA_HOST: grafana.observability.svc.cluster.local
              GRAFANA_PORT: 80
              GRAFANA_ONCALL_HOST: grafana-oncall.observability.svc.cluster.local
              GRAFANA_ONCALL_PORT: 8080
            envFrom:
              - secretRef:
                  name: grafana-oncall-initialize-secret
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 25m
              limits:
                memory: 256M
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        seccompProfile: { type: RuntimeDefault }
    persistence:
      scripts:
        type: configMap
        name: grafana-oncall-initialize
        defaultMode: 0775
        globalMounts:
          - path: /scripts
            readOnly: true
